export type client = {
    name: string;
    id: number;
    cod_categories_client: number;
  }

export type Relational = {
    cod_clients: number,
    cod_relational: number,
    id: number

}

export type relationalData {
    cod_clients: number,
    cod_relational: number,
}